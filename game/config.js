// Endless Arena — Backend configuration
  // -----------------------------------------------
  // BACKEND_URL points to the Replit deployed API/WebSocket server.
  // Images and sounds are now served from GitHub Pages (same origin).
  // -----------------------------------------------
  window.BACKEND_URL = 'https://arena-survival--JemjemfromYT.replit.app';
  